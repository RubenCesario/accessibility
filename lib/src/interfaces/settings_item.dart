import 'package:flutter/widgets.dart' show StatefulWidget;

/// Define a family of [SettingsItem]s, to limits their uses.
abstract class SettingsItem extends StatefulWidget {
  /// Creates a [SettingsItem] Widget.
  const SettingsItem({super.key});
}
