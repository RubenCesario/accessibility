/// The minimum value of the word spacing text slider.
const double kMinWordSpacingSliderValue = 0.5;

/// The maximum value of the word spacing text slider.
const double kMaxWordSpacingSliderValue = 3;

/// The minimum value of the line height text slider.
const double kMinLineHeightSliderValue = 1.5;

/// The maximum value of the line height text slider.
const double kMaxLineHeightSliderValue = 2;

/// The minimum value of the letter spacing text slider.
const double kMinLetterSpacingSliderValue = 0.5;

/// The maximum value of the letter spacing text slider.
const double kMaxLetterSpacingSliderValue = 1;

/// The minimum value of the scale factor text slider.
const double kMinScaleFactorSliderValue = 1;

/// The maximum value of the scale factor text slider.
const double kMaxScaleFactorSliderValue = 2;

/// The number of divisions of the text sliders.
const int kSliderDivisions = 4;
